import { StyleSheet } from 'react-native'

/**
 * Данный файл содержит базовые стили приложения по умолчанию
 */
export default StyleSheet.create({
  container: {
    flex: 1,
  },
  backgroundImage: {
    justifyContent: 'center',
    resizeMode: 'cover',
  },
})
